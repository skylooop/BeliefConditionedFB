# @package _global_

defaults:
  - override /env: gridworld/maze-fourrooms-11
  - override /agent: dynamics_aware_fb

train_steps: 1_000_000
# For debug
log_interval: 5_000
eval_interval: 10_000
tags: [dynamical_fb, discrete]

agent:
  lr: 3e-4
  batch_size: 1024

  # FB Specific
  z_dim: &z_dim 150
  z_mix_ratio: 0.5
  boltzmann: True

  # Transformer
  warmup_steps: 100
  n_blocks: 2
  n_heads: 2
  h_dim: *z_dim
  context_len: 512
  number_of_meta_envs: 5
  use_masked_attention: False
  use_mean_embedding: True
  cosine_schedule: False

  # F Network
  f_hidden_dims: [512, 512, 512]  # Value network hidden dimensions.
  f_layer_norm: True  # Whether to use layer normalization.
  # B Network
  b_hidden_dims: [512, 512, 512]  # Value network hidden dimensions.
  b_layer_norm: True  # Whether to use layer normalization.

  # in discrete case actor is deterministic
  # MISC
  discount: 0.99
  tau: 0.01  # Target network update rate.
  
